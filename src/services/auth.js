export class AuthService {
    login() {
        try {
            // 사용자의 정보를 입력받아 데이터 베이스에 이미 존재하는 데이터인지 확인
        } catch (err) {
            console.log(err)
        }
    }

    signUp() {
        try {
            // 회원 가입, 데이터 베이스에 회원 정보 저장
        } catch (err) {
            console.log(err)
        }
    }
}
